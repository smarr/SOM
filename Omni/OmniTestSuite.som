OmniTestSuite = (
	
	tests = (
		^ OstDomainTest, ONODomainTest, ONOMirrorTest
	)

	run = (
		self tests do: [:t |
			| runner |
			runner := TestRunner new.
			runner initializeOn: t.
			runner run.
		]
	)
)
